# Start: docker compose up -d
# Attu: http://localhost:9095 | Milvus: localhost:19530
# VPS access: ssh -L 9095:127.0.0.1:9095 -L 19530:127.0.0.1:19530 user@your-vps
# Named volumes retain data across restarts and docker compose down.
# docker compose down -v permanently removes the stored data.

x-milvus-image: &milvus-image milvusdb/milvus:v3.0.2
# Digest verified by pulling and running on the ARM64 VPS.
x-minio-image: &minio-image docker.io/bitnamilegacy/minio@sha256:451fe6858cb770cc9d0e77ba811ce287420f781c7c1b806a386f6896471a349c

services:
  # Remove the .milvus-volume-owner-* marker after restoring files owned by a
  # different user so this one-shot migration runs again.
  milvus-volume-init:
    image: *milvus-image
    user: "0:0"
    network_mode: none
    read_only: true
    restart: "no"
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -eu
        uid="$$(id -u milvus)"
        gid="$$(id -g milvus)"
        marker="/var/lib/milvus/.milvus-volume-owner-$${uid}-$${gid}"
        if [ ! -e "$${marker}" ]; then
          echo "Preparing /var/lib/milvus for $${uid}:$${gid}"
          chown -R "$${uid}:$${gid}" /var/lib/milvus
          touch "$${marker}"
          chown "$${uid}:$${gid}" "$${marker}"
        fi
    volumes:
      - milvus-data:/var/lib/milvus

  etcd:
    restart: unless-stopped
    image: quay.io/coreos/etcd:v3.5.25
    environment:
      - ETCD_AUTO_COMPACTION_MODE=revision
      - ETCD_AUTO_COMPACTION_RETENTION=1000
      - ETCD_QUOTA_BACKEND_BYTES=4294967296
      - ETCD_SNAPSHOT_COUNT=50000
    volumes:
      - etcd-data:/etcd
    command: etcd -advertise-client-urls=http://etcd:2379 -listen-client-urls http://0.0.0.0:2379 --data-dir /etcd
    healthcheck:
      test: ["CMD", "etcdctl", "endpoint", "health"]
      interval: 30s
      timeout: 20s
      retries: 3

  minio-volume-init:
    image: *minio-image
    user: "0:0"
    network_mode: none
    read_only: true
    restart: "no"
    entrypoint: ["/bin/sh", "-c"]
    command: ["chown -R 1001:0 /bitnami/minio/data"]
    volumes:
      - minio-data:/bitnami/minio/data

  minio:
    restart: unless-stopped
    image: *minio-image
    user: "1001:0"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
      MC_HOST_local: http://minioadmin:minioadmin@127.0.0.1:9000
    volumes:
      - minio-data:/bitnami/minio/data
    # Launch the verified binary directly, bypassing Bitnami setup scripts.
    entrypoint: ["minio"]
    command: ["server", "/bitnami/minio/data", "--address", ":9000", "--console-address", ":9001"]
    depends_on:
      minio-volume-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 30s
      start_period: 30s
      timeout: 20s
      retries: 3

  standalone:
    restart: unless-stopped
    image: *milvus-image
    command: ["milvus", "run", "standalone"]
    security_opt:
    - seccomp:unconfined
    environment:
      MINIO_REGION: us-east-1
      ETCD_ENDPOINTS: etcd:2379
      MINIO_ADDRESS: minio:9000
    volumes:
      - milvus-data:/var/lib/milvus
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9091/healthz"]
      interval: 30s
      start_period: 90s
      timeout: 20s
      retries: 3
    ports:
      - "127.0.0.1:19530:19530"
      - "127.0.0.1:9091:9091"
    depends_on:
      milvus-volume-init:
        condition: service_completed_successfully
      etcd:
        condition: service_healthy
      minio:
        condition: service_healthy

  attu:
    image: zilliz/attu:v3.0.0
    restart: unless-stopped
    environment:
      MILVUS_ADDRESS: standalone:19530
      MILVUS_NAME: Milvus Standalone
    ports:
      - "127.0.0.1:9095:3000"
    volumes:
      - attu-data:/data
    depends_on:
      standalone:
        condition: service_healthy

volumes:
  etcd-data:
  minio-data:
  milvus-data:
  attu-data:
